services:
  sim:
    build:
      context: .
      args:
        ARDUPILOT_REF: ${ARDUPILOT_REF:-Copter-4.6.3}
        WITH_RTABMAP: ${WITH_RTABMAP:-1}
    image: ghcr.io/uc-ufo/wh-hackathon-sim:latest
    container_name: wh-sim
    tty: true
    stdin_open: true

    network_mode: "host"
    ipc: "host"

    security_opt:
      - "seccomp:unconfined"
      - "apparmor:unconfined"

    environment:
      - DISPLAY
      - XAUTHORITY=/home/ubuntu/.Xauthority
      - QT_X11_NO_MITSHM=1
      - NVIDIA_DRIVER_CAPABILITIES=graphics,utility,compute,display
      - NVIDIA_VISIBLE_DEVICES=all
      - __GLX_VENDOR_LIBRARY_NAME=nvidia
      - __NV_PRIME_RENDER_OFFLOAD=1
      - FASTRTPS_DEFAULT_PROFILES_FILE=/workspace/wh-hackathon/config/fastdds.xml

    # The AR preview's --camera. Defaulting to /dev/null keeps the container
    # startable on a machine with no camera, where opening it simply fails.
    devices:
      - ${CAMERA:-/dev/null}:/dev/video0

    # By number, not by name: the host's video group is gid 39 on Fedora and the
    # container calls that gid irc. run.sh reads it off the device itself.
    group_add:
      - "${VIDEO_GID:-44}"

    volumes:
      - .:/workspace/wh-hackathon
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${XAUTHORITY:-$HOME/.Xauthority}:/home/ubuntu/.Xauthority:ro

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, utility, compute, graphics, display]
